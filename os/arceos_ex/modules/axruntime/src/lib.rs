//! arceos_ex runtime skeleton.
//!
//! The runtime will orchestrate the specification-defined startup phases. The
//! current crate exists so the overlay workspace can resolve the arceos_ex
//! dependency graph before the runtime implementation is introduced.

#![no_std]
